services:
  - type: web
    name: flask-recommendations
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PORT
        value: 10000
      - key: MONGO_URL
        sync: false
